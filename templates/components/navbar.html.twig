<nav class="layout-navbar container-xxl navbar navbar-expand-xl navbar-detached align-items-center bg-navbar-theme" id="layout-navbar">
	<div class="layout-menu-toggle navbar-nav align-items-xl-center me-3 me-xl-0 d-xl-none">
		<a class="nav-item nav-link px-0 me-xl-4 {{ app.user.roles[0] == 'ROLE_ADMIN' ? '' : 'd-none' }}" href="javascript:void(0)">
			<i class="bx bx-menu bx-sm"></i>
		</a>
	</div>
	<div class="navbar-nav-right d-flex align-items-center" id="navbar-collapse">
		<form action="" class="navbar-nav align-items-center">
			<div class="nav-item d-flex align-items-center">
				<i class="bx bx-search fs-4 lh-0"></i>
				<input type="text" name="query" class="form-control border-0 shadow-none" placeholder="Search..." aria-label="Search..."/>
			</div>
		</form>
		<ul class="navbar-nav flex-row align-items-center ms-auto">
			<li class="nav-item navbar-dropdown dropdown-user dropdown">
				<a class="nav-link dropdown-toggle hide-arrow" href="javascript:void(0);" data-bs-toggle="dropdown">
					<div class="avatar avatar-online">
						<img src="{{ asset(app.user.imagePath != null ? app.user.imagePath : 'assets/img/avatars/no-avatar.png') }}" alt class="w-px-40 avatar"/>
					</div>
				</a>
				<ul class="dropdown-menu dropdown-menu-end">
					<li>
						<a class="dropdown-item" href="#">
							<div class="d-flex">
								<div class="flex-shrink-0 me-3">
									<div class="avatar  rounded-cercle avatar-online">
										<img class="avatar" src="{{ asset(app.user.imagePath != null ? app.user.imagePath : 'assets/img/avatars/no-avatar.png') }}" alt="">
									</div>
								</div>
								<div class="flex-grow-1">
									<span class="fw-semibold d-block">{{ app.user.firstName ~ ' ' ~ app.user.lastName}}</span>
									<small class="text-muted">
										{% if app.user.roles[0] == 'ROLE_ADMIN' %}
											Admin
										{% elseif app.user.roles[0] == 'ROLE_MANAGER' %}
											Manager
										{% else %}
											Member
										{% endif %}
									</small>
								</div>
							</div>
						</a>
					</li>
					<li>
						<div class="dropdown-divider"></div>
					</li>
					{% if app.user.roles[0] == 'ROLE_MANAGER' %}
						<li>
							<a class="dropdown-item" href="{{ path('manager') }}">
								<i class='bx bx-tachometer me-2'></i>
								<span class="align-middle">Dashboard</span>
							</a>
						</li>
					{% endif %}
					{% if app.user.roles[0] == 'ROLE_USER' %}
						<li>
							<a class="dropdown-item" href="{{ path('member') }}">
								<i class='bx bx-tachometer me-2'></i>
								<span class="align-middle">Dashboard</span>
							</a>
						</li>
					{% endif %}
					<li>
						<a class="dropdown-item" href="{{ path('profile') }}">
							<i class="bx bx-user me-2"></i>
							<span class="align-middle">My Profile</span>
						</a>
					</li>
					<li>
						<a class="dropdown-item" href="{{ path('inbox') }}">
							<i class='bx bxs-inbox me-2'></i>
							<span class="align-middle">Inbox</span>
						</a>
					</li>
					<li>
						<div class="dropdown-divider"></div>
					</li>
					<li>
						<a class="dropdown-item" href="{{ path('app_logout') }}">
							<i class="bx bx-power-off me-2"></i>
							<span class="align-middle">Log Out</span>
						</a>
					</li>
				</ul>
			</li>
		</ul>
	</div>
</nav>
