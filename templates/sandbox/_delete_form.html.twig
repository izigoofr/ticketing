<form method="post" action="{{ path('app_sandbox_delete', {'id': sandbox.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sandbox.id) }}">
    <button class="btn">Delete</button>
</form>
